import { NgClass2Directive } from './ng-class2.directive';

describe('NgClass2Directive', () => {
  it('should create an instance', () => {
    const directive = new NgClass2Directive();
    expect(directive).toBeTruthy();
  });
});
